# Optional. Without these the demo runs with a deterministic mock brain (no API call).
# Set them to use the real Opus brain for drafting.
ANTHROPIC_API_KEY=sk-ant-...
# WA_BRAIN_MODEL=claude-opus-4-8   # override: claude-sonnet-4-6 / claude-haiku-4-5 for cost
